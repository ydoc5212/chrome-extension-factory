## Summary

<!-- One or two sentences — what changes and why. -->

## Test plan

- [ ] `claude plugin validate .` passes
- [ ] Smoke-tested via `claude --plugin-dir .` (skills appear under `/help`)
- [ ] `npm run check:cws` still green (factory structural rules intact)
- [ ] README / CHANGELOG updated if user-visible behavior changed
- [ ] Version bumped in `.claude-plugin/plugin.json` if this is a release
